extends layout.pug

block content
    h1= book.title
    h2= book.Author.name
    h3= book.Genre.name
    img(src=book.coverPhoto alt='test')

    if book.bookReviews.length
        each review in reviews
            h3= review.username
            h3= review.reviewHeader
            h3= review.reviewBody
