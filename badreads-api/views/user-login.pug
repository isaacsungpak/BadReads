extends layout.pug

include utils.pug

block content
    +validationErrorSummary(errors)
    form(method="post" action="/users/login")
        +field("Username", "username", username, "text", "clubPenguinGuy118")
        +field("Password", "password", "", "password")
        input(name="_csrf" type="hidden" value=csrfToken)
        button Log In
